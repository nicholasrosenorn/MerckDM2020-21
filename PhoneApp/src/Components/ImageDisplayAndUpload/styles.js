import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  userHeadShot: {
    width: 200,
    height: 200,
    borderRadius: 200,
    marginBottom: 10,
    marginTop: 10
  }
});

export default styles;